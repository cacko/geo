import { LocationBgDirective } from './location-bg.directive';

describe('LocationBgDirective', () => {
  it('should create an instance', () => {
    const directive = new LocationBgDirective();
    expect(directive).toBeTruthy();
  });
});
